{{#each posts as |post|}}
<div class="card"><a href="/dashboard/post/{{post.id}}">
        <div class="card-header">
            <h2>{{post.title}}</h2>
            <p id="timePosted">{{post.createdAt}}</p>
        </div>
        {{!-- <div class="card-body">
            <p>{{post.body}}</p>
        </div> --}}
    </a>
</div>
{{/each}}